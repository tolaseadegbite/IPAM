examples:
  periodic_cleanup:
    class: CleanSoftDeletedRecordsJob
    queue: background
    args: [ 1000, { batch_size: 500 } ]
    schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day



# This applies to ALL environments (Dev and Prod)
# network_scan paused: re-enable once real subnets are inventoried.
# Manual scans still work via the Scan Now button; NAT's FindFreeIps
# answers provisioning queries from current DB state.
# network_scan:
#   class: NetworkScanJob
#   queue: monitoring
#   schedule: "every 5 minutes"

clear_solid_queue_finished_jobs:
  command: "SolidQueue::Job.clear_finished_in_batches(sleep_between_batches: 0.3)"
  schedule: every hour at minute 12

# Keep the PaperTrail audit log bounded (default: 12 months of history).
versions_prune:
  class: VersionsPruneJob
  queue: maintenance
  schedule: every day at 3am
