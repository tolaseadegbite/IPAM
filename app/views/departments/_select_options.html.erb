<%# The ID must match the frame in the parent form %>
<%= turbo_frame_tag "department_options_frame" do %>
  
  <%# We use select_tag here because we don't have the 'form' builder object 
      from the parent EmployeesController. 
      
      CRITICAL: We must manually set the 'name' to "employee[department_id]" 
      so that when the main form submits, it looks just like a native Rails form field. 
  %>
  <%= select_tag "employee[department_id]", 
        options_from_collection_for_select(departments, :id, :name),
        prompt: "Select Department",
        class: "input w-full p-2 border rounded cursor-pointer",
        id: "employee_department_id" %>

<% end %>