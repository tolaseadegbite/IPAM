<% content_for :title, "Edit Employee" %>

<%= render "shared/modal" do %>
  <div data-action="turbo:submit-end->modal#submitEnd">
    <h1 class="font-bold text-2xl mbe-4">Edit Employee</h1>
    <%= render "form", employee: @employee %>
  </div>
<% end %>