<% content_for :title, "New Board" %>

<%= render "shared/modal" do %>
  <div data-action="turbo:submit-end->modal#submitEnd">
    <h1 class="font-bold text-2xl mbe-4">Create New Board</h1>
    <%= render "form", board: @board %>
  </div>
<% end %>