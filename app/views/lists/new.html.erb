<% content_for :title, "New Column" %>

<%= render "shared/modal" do %>
  <div data-action="turbo:submit-end->modal#submitEnd">
    <h1 class="font-bold text-2xl mbe-4">Create New Column</h1>
    <%= render "form", board: @board, list: @list %>
  </div>
<% end %>