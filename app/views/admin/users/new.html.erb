<% content_for :title, "New User" %>

<%= render "shared/modal" do %>
  <div data-action="turbo:submit-end->modal#submitEnd">
    <h1 class="font-bold text-2xl mbe-4">New User</h1>
    <%= render "form", user: @user %>
  </div>
<% end %>